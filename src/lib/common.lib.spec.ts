// import { equal } from 'node:assert/strict'

// import { generateOtp, generateBase64Url } from './common.lib'

// describe('Api Utilities', function () {
//   it('Generate OTP', function () {
//     equal(typeof generateOtp(), 'number', 'must be number')
//     equal(generateOtp().toString().length, 4, 'must be 4 digit')
//   })

//   it('Generate random string', function () {
//     const uniqueString = generateBase64Url()
//     equal(typeof uniqueString, 'string', 'must be string')
//   })
// })
